const items = ['item1', 'item2', 'item3']; 

module.exports.item = items; 

const person = {
    name: 'muaz', 
    age: 18, 
    degree: 'BESE'
}

module.exports.singlePerson = person; 

// two exports do the same as one export with multiple data (returns an objcet of all items)